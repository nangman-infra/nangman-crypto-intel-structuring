# Legacy local compose file.
#
# Do not use this file for deployment or runtime verification. The current
# intel-structuring runtime is deployed through AWS ECS task definitions and IAM
# access to AWS S3.

name: nangman-crypto-intel-structuring-legacy-compose-disabled

services: {}
